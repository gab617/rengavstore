# Misma URL y key ANON del proyecto admin (comercioFrontWEB/.env.local).
# La key anon es segura de exponer: la DB solo deja llamar a las RPCs del storefront.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
